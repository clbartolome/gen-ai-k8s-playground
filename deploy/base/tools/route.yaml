apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: tools
  labels:
    app.kubernetes.io/name: tools
    app.kubernetes.io/component: tools
  annotations:
    openshift.io/description: "Tools hub UI; platform MCP API on port 9001 (ITSM/KB via itsm-app)"
spec:
  to:
    kind: Service
    name: tools
  port:
    targetPort: hub
