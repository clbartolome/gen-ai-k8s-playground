apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: agent
  labels:
    app.kubernetes.io/name: agent
    app.kubernetes.io/component: agent
  annotations:
    openshift.io/description: "Public URL for agent debug UI (/ redirects to /debug)"
spec:
  to:
    kind: Service
    name: agent
  port:
    targetPort: http
