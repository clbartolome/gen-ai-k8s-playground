apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: tools
  labels:
    app.kubernetes.io/name: tools
    app.kubernetes.io/component: tools
  annotations:
    openshift.io/description: "Tools hub UI; MCP/ITSM/RAG APIs are cluster-internal on ports 9001-9003"
spec:
  to:
    kind: Service
    name: tools
  port:
    targetPort: hub
